import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    homeHead: {
    flex: 1,
    justifyContent: 'top',
    padding: 20,
    fontSize: 30,
  }
});

export default styles;